name 'iproute2'
org 'amylum'

licenses 'COPYING'

deps(
  iptables: '1.6.1-5',
  'linux-api-headers': '4.11-2'
)

configure_flags(
  prefix: '/usr'
)

cflags
harden

patch '0001-in-in6.patch'

build do
  configure
  make
  install
  run "mv #{releasedir}/sbin #{releasedir}/usr/bin"
end

test do
  # TODO: add tests
end
