Prospectus.extra_dep('file', 'pkgforge')
Prospectus.extra_dep('file', 'prospectus_pkgforge')

item do
  expected do
    url_xpath
    url 'http://git.kernel.org/pub/scm/linux/kernel/git/shemminger/iproute2.git/refs/tags'
    xpath '//*[@id="cgit"]/div/table/tr[2]/td[1]/a'
    regex /^v([\d.]+)$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end

  extend ProspectusPkgforge::DepItems
end
